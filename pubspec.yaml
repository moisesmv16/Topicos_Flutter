name: fluter_aplication_1
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  another_flutter_splash_screen: ^1.2.0
  url_launcher: ^6.1.14
  image_picker: ^1.0.2
  carousel_slider: ^4.0.0
  lottie: ^1.0.0
  http: ^1.1.0
  concentric_transition: ^1.0.1+1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
